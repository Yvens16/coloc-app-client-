import React, { Component } from "react";

class SearchBar extends Component {
  render() {
    return (
      <section>
        <input type="search" />
      </section>
    );
  }
}

export default SearchBar;
